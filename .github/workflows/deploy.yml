# name: Deploy Frontend (hayat)

# on:
#   push:
#     branches:
#       - hayat

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1️⃣ Checkout code
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # 2️⃣ Build Docker image on GitHub runner
#       - name: Build Docker image
#         run: |
#           docker build -t rbwoodruff-frontend:latest .

#       # 3️⃣ Save Docker image to tar
#       - name: Save Docker image
#         run: |
#           docker save rbwoodruff-frontend:latest -o rbwoodruff-frontend.tar

#       # 4️⃣ Fix permissions (VERY IMPORTANT)
#       - name: Fix image permissions
#         run: |
#           chmod 644 rbwoodruff-frontend.tar

#       # 5️⃣ Copy image to server
#       - name: Copy image to server
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           source: "rbwoodruff-frontend.tar"
#           target: "/root/rbwoodruff_frontend"

#       # 6️⃣ SSH into server and deploy
#       - name: Deploy on server
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           script: |
#             cd /root/rbwoodruff_frontend
#             docker load -i rbwoodruff-frontend.tar
#             docker compose down
#             docker compose up -d
